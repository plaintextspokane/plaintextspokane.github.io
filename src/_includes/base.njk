<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>{{ title or "Plaintext Spokane" }}</title>
  <link rel="stylesheet" href="/assets/style.css">
</head>
<body>

<nav>
  <a href="/">Home</a>
  {% if page.url is defined and page.url != "/" %}
    {% set crumbs = page | breadcrumbs %}
    {% if crumbs %}
      {% for c in crumbs %}
        &raquo; <a href="{{ c.url }}">{{ c.name }}</a>
      {% endfor %}
    {% endif %}
  {% endif %}
</nav>

<main>
  {{ content | safe }}
</main>

</body>
</html>
